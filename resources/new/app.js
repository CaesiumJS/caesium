module.exports = {
  layouts: {
    // Add each of your layouts in the form name: require('./path/to/layout')
    // page: require('./page') you can then use this with layout: 'page' in your front matter
  }
}
